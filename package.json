{
  "name": "cc-baseline",
  "version": "1.3.1",
  "description": "Claude Code harness bundle installer — wires up behavior rules, custom skills, E2E agents, and hook config in a single command",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "cc-baseline": "./bin/cli.js"
  },
  "scripts": {
    "start": "node bin/cli.js",
    "test": "node test/run.js"
  },
  "files": [
    "bin/",
    "src/",
    "templates/"
  ]
}
